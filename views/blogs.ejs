<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Document</title>
    <link rel="stylesheet" href="/css/style.css" />
  </head>
  <body>
    <nav>
      <div class="back"><a href="/app/v1/user/dashboard">Home</a></div>
      <ul>
        <li><a href="/app/v1/user/logout">Logout</a></li>
      </ul>
    </nav>
    <main>
      <form>
        <input type="text" name="search" id="search" />
        <input type="submit" value="search" />
      </form>
      <div class="blogs">
        <% blogs.forEach(blog=>{%>
        <a href="/app/v1/blogs/<%= blog._id%>">
          <div class="blog">
            <h2 class="blog-title"><%= blog.title%></h2>
            <p><%= blog.snippet%></p>
            <span>Price:<%=blog.price%></span>
            <p>By-<%= blog.author.name%></p>
            <a href="/app/v1/payment/create-checkout-session/<%=blog._id%>"
              >Buy this</a
            >
          </div>
        </a>
        <%})%>
      </div>
      <a href="?page=<%=previous%>">previous</a>
      <%for(let i=1;i<=totalPages;i++){%>
      <a href="?page=<%=i%>"><%=i%></a>
      <%}%>
      <a href="?page=<%=next%>">next</a>
    </main>
  </body>
</html>
